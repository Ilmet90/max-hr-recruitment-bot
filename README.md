# MAX HR Recruitment Bot

Шаблон чат-бота MAX и web-панели для кадрового подразделения организации.

Проект помогает публиковать вакансии, принимать отклики кандидатов, вести вопросы и сообщения, показывать условия работы или службы, управлять фотоальбомами и распределять отклики между сотрудниками кадрового подразделения.

Версия 0.2.0 — добавлен импорт вакансий с портала «Работа России», предпросмотр, синхронизация и очистка служебных полей.

## Возможности

- публичное меню для кандидатов в MAX;
- список вакансий и карточки вакансий;
- импорт вакансий с портала «Работа России»;
- пошаговый отклик на вакансию;
- вопросы кандидатов;
- сообщения через бота;
- раздел условий работы или службы;
- фотоальбомы с отправкой изображений через MAX;
- локальная web-админка на FastAPI и Jinja2;
- сотрудники кадрового подразделения;
- роли и подтверждение доступа;
- принятие отклика в работу;
- назначение откликов сотрудникам;
- изменение служебного кода `/admin`;
- настройка организации, публичных текстов и цветов интерфейса;
- раздел «О программе» с проверкой обновлений и обслуживанием;
- автозапуск через systemd.

## Требования

- Debian 12/13 или Ubuntu Server;
- Python 3;
- доступ в интернет для MAX API;
- токен MAX-бота.

## Быстрая установка

```bash
git clone <repo-url>
cd max-hr-recruitment-bot
sudo bash install.sh
```

Установщик задаст вопросы об организации, главной web-учётной записи, служебном коде `/admin`, MAX-токене, host и port web-админки. Пароль главной web-учётной записи, служебный код и `MAX_BOT_TOKEN` вводятся обычным видимым текстом. Если оставить пароль или служебный код пустыми, установщик сгенерирует их и покажет один раз в конце установки. `MAX_BOT_TOKEN` можно оставить пустым и заполнить позже в `/opt/max-hr-recruitment-bot/.env`.

## Первый вход

После установки откройте web-интерфейс:

```text
http://<IP_сервера>:<APP_PORT>
```

Логин задаётся при установке. Пароль задаётся видимым текстом при установке или генерируется автоматически, если оставить поле пустым; сгенерированное значение показывается один раз в конце установки.

## Настройка MAX-токена

Если токен был указан при установке, служба бота запускается автоматически.

Если токен не был указан:

1. Откройте файл `/opt/max-hr-recruitment-bot/.env`.
2. Заполните `MAX_BOT_TOKEN`.
3. Запустите службу:

```bash
sudo systemctl start max-hr-bot.service
sudo systemctl enable max-hr-bot.service
```

## Регистрация сотрудников

Сотрудник пишет боту:

```text
/admin <служебный код>
```

Заявка получает статус «Ожидает подтверждения». Главная учётная запись или начальник кадрового подразделения подтверждает пользователя в web-админке или через служебное меню `/staff`.

## Роли

- Главная учётная запись — резервный web-вход и полный доступ.
- Начальник кадрового подразделения — подтверждает сотрудников, управляет ролями, назначает отклики и делегирует полномочия.
- Сотрудник кадрового подразделения — принимает отклики в работу, видит свои и общие отклики, редактирует контент.
- Ожидает подтверждения — заявка на доступ ещё не одобрена.
- Отключён — доступ отключён.

## Настройка организации

Главная учётная запись может открыть «Настройки организации» и изменить:

- полное и краткое название организации;
- вышестоящую организацию;
- регион;
- название бота;
- тексты приветствия;
- контакты;
- сообщения кандидату;
- предупреждение о персональных данных;
- заголовки web-админки;
- цвета интерфейса.

После сохранения новые значения используются в web-админке, приветствии бота, сообщениях кандидату, уведомлениях, условиях и контактах.

Служебные имена systemd и папка проекта могут оставаться прежними.

## Импорт вакансий с портала Работа России

В web-админке откройте «Вакансии → Импорт с Работа России». Укажите ИНН работодателя или код работодателя на портале, затем нажмите «Проверить вакансии». Система покажет предпросмотр: название, зарплату, обязанности, требования, статус импорта и ссылку на исходную карточку, если она есть.

После проверки нажмите «Импортировать найденные вакансии». Повторный импорт обновляет ранее импортированные вакансии по `external_source='trudvsem'` и `external_id`, но не удаляет вакансии автоматически, если они исчезли на портале. Вручную созданные вакансии не перезаписываются.

При нормализации адреса, координаты, контактные лица и телефоны из внешнего API не публикуются в карточках вакансий. В примечании остаётся только источник «Работа России» и ссылка на вакансию. Исходный JSON сохраняется в служебном поле `external_raw_json`, чтобы при необходимости можно было переобработать данные.

Если вакансии были импортированы до изменения правил очистки, нажмите «Очистить и обновить уже импортированные вакансии». Система заново обработает `external_raw_json`, не меняя ручную активность и порядок сортировки.

Синхронизация также доступна во внутреннем меню MAX-бота: `/staff → Вакансии → Синхронизировать с Работа России`. Если ИНН и код работодателя не заполнены, бот попросит настроить импорт в web-админке.

## Управление службами

```bash
sudo systemctl status max-hr-admin.service
sudo systemctl status max-hr-bot.service
sudo systemctl restart max-hr-admin.service
sudo systemctl restart max-hr-bot.service
sudo journalctl -u max-hr-admin.service -f
sudo journalctl -u max-hr-bot.service -f
```

## О программе, обновления и обслуживание

В web-админке есть раздел «О программе». Обычный сотрудник видит сведения о версии. Главная учётная запись, начальник кадрового подразделения и временно делегированный начальник могут:

- проверить обновления на GitHub;
- обновить код из GitHub;
- перезапустить web-админку;
- перезапустить MAX-бота.

Аналогичный раздел доступен во внутреннем меню `/staff` по кнопке «О программе». Для опасных действий бот просит подтверждение.

Перед использованием кнопок обновления и перезапуска нужно один раз настроить ограниченные права sudo:

```bash
cd /opt/max-hr-recruitment-bot
sudo bash scripts/setup_maintenance_sudoers.sh
```

Скрипт разрешает пользователю сервиса выполнять без пароля только `start`, `stop`, `restart` и `status` для служб `max-hr-admin.service` и `max-hr-bot.service`. Не используется `NOPASSWD:ALL`.

При обновлении через `scripts/update_from_github.sh` сохраняются:

```text
.env
data/
logs/
backups/
app/static/uploads/
```

Перед заменой кода создаётся резервная копия SQLite-базы и загруженных файлов. Настройки обновления хранятся в SQLite в таблице `settings`: `github_repo_url`, `github_branch`, `installed_commit`, `admin_service_name`, `bot_service_name`, `install_path`.

## Резервное копирование

В установленном проекте:

```bash
cd /opt/max-hr-recruitment-bot
sudo bash scripts/backup.sh
```

Архив включает SQLite-базу и загруженные файлы.

## Обновление

В установленном проекте:

```bash
cd /opt/max-hr-recruitment-bot
bash scripts/update_from_github.sh
```

Скрипт останавливает службы, делает резервную копию базы и загруженных файлов, получает свежий код из доверенного GitHub-репозитория, устанавливает зависимости, выполняет миграции и запускает службы.

## Безопасность

- Не публикуйте `.env`.
- Не храните токены и пароли в репозитории.
- Используйте сложные пароли.
- Меняйте служебный код `/admin`.
- Не открывайте web-админку в интернет без HTTPS и дополнительной защиты.
- Используйте VPN, SSH-туннель или внутреннюю сеть.
- Регулярно делайте резервные копии.
- При утечке MAX-токена перевыпустите токен.
- При увольнении сотрудника отключайте его учётную запись.
- Не размещайте в публичных текстах сведения ограниченного доступа, персональные данные сотрудников или служебную информацию, не предназначенную для публикации.

## Лицензия

По умолчанию добавлена MIT License. Организация может заменить лицензию под свои требования.
